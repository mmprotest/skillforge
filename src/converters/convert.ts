import type { Diagnostic } from '../diagnostics.js';
import type { NormalizedWorkflow, TargetName } from '../normalize.js';
import { VALID_TARGETS } from '../normalize.js';
import type { SourceFormat } from '../importers/types.js';
export function defaultTargets(source: SourceFormat): TargetName[] { return VALID_TARGETS.filter(t => t !== source); }
export function conversionDiagnostics(w: NormalizedWorkflow, source: SourceFormat, targets: TargetName[], metadata:Record<string,unknown>={}): Diagnostic[] { const d:Diagnostic[]=[]; for (const t of targets) { if ((source==='claude'||source==='codex') && t==='copilot') d.push({severity:'warning', path:'conversion.semanticLoss', target:t, message:'Source skill may be invoked on demand, but GitHub Copilot instructions are persistent path-specific guidance. Invocation behavior cannot be preserved exactly.'}); if (w.safety.allowedTools.length && ['copilot','agents','cursor'].includes(t)) d.push({severity:'warning', path:'conversion.allowedTools', target:t, message:'Source defines allowed tools. Target cannot enforce tool permissions. Generated instructions include them as text guidance only.'}); if (source==='cursor' && metadata.alwaysApply === true && ['claude','codex'].includes(t)) d.push({severity:'warning', path:'conversion.activation', target:t, message:'Cursor alwaysApply behavior cannot be represented exactly as a Claude/Codex skill.'}); if (source==='agents' && metadata.freeform) d.push({severity:'warning', path:'conversion.lowStructure', target:t, message:'AGENTS.md is freeform repository guidance. Generated target artifacts are a conservative approximation.'}); } return d; }
