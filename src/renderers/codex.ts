import type { NormalizedWorkflow } from '../normalize.js'; import type { GeneratedFile } from './shared.js'; import { md, notice } from './shared.js';
export function renderCodex(w:NormalizedWorkflow, src='workflow.yaml'):GeneratedFile[]{ return [{path:`.agents/skills/${w.id}/SKILL.md`,content:`${notice(src)}\n---\nname: ${w.id}\ndescription: ${w.description}\n---\n\n${md(w,{when:true})}`,target:'codex'}, {path:`.agents/skills/${w.id}/agents/openai.yaml`,content:`# Generated by skillforge from ${src}. Edit the workflow spec instead.\nskill: ${w.id}\nversion: ${w.version}\n`,target:'codex'}]; }
