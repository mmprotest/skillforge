import type { NormalizedWorkflow } from '../normalize.js'; import type { GeneratedFile } from './shared.js'; import { md, notice } from './shared.js';
export function renderCopilot(w:NormalizedWorkflow, src='workflow.yaml'):GeneratedFile[]{ const apply=[...w.triggers.globs,...w.scope.paths].join(',') || '**'; return [{path:`.github/instructions/${w.id}.instructions.md`,content:`${notice(src)}\n---\napplyTo: "${apply}"\n---\n\n${md(w,{when:false,concise:true})}`,target:'copilot'}]; }
