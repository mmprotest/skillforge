import type { NormalizedWorkflow } from '../normalize.js'; import type { GeneratedFile } from './shared.js';
export const begin=(id:string)=>`<!-- skillforge:${id} BEGIN -->`; export const end=(id:string)=>`<!-- skillforge:${id} END -->`; export function managedSection(w:NormalizedWorkflow){ const l=[begin(w.id),'<!-- Generated by skillforge. Do not edit this managed section directly. -->',`## ${w.title}`,'',`Goal: ${w.workflow.goal}`]; if(w.scope.paths.length) l.push('', 'Relevant paths:', ...w.scope.paths.map(p=>`- ${p}`)); if(Object.keys(w.commands).length) l.push('', 'Commands:', ...Object.entries(w.commands).flatMap(([k,cs])=>[`- ${k}: ${cs.map(c=>`\`${c}\``).join(', ')}`])); if(w.safety.never.length) l.push('', 'Safety:', ...w.safety.never.map(x=>`- ${x}`)); if(w.outputs.successCriteria.length) l.push('', 'Success criteria:', ...w.outputs.successCriteria.map(x=>`- ${x}`)); l.push(end(w.id)); return l.join('\n')+'\n'; }
export function renderAgents(w:NormalizedWorkflow):GeneratedFile[]{ return [{path:'AGENTS.md', content:managedSection(w), target:'agents'}]; }
