import type { RawWorkflow } from './schema.js';
export type TargetName = 'claude'|'codex'|'agents'|'cursor'|'copilot';
export type NormalizedWorkflow = { id:string; title:string; description:string; version:string; triggers:{phrases:string[];globs:string[];manual:boolean}; scope:{paths:string[]}; inputs:Array<{name:string;optional:boolean;description?:string}>; workflow:{goal:string;steps:string[]}; commands:Record<string,string[]>; safety:{allowedTools:string[];never:string[]}; outputs:{successCriteria:string[]}; targets:Record<TargetName,{enabled:boolean}> };
export function normalizeWorkflow(w: RawWorkflow): NormalizedWorkflow { return { id:w.id, title:w.title, description:w.description, version:w.version ?? '0.1.0', triggers:{ phrases:w.triggers?.phrases ?? [], globs:w.triggers?.globs ?? [], manual:w.triggers?.manual ?? false }, scope:{ paths:w.scope?.paths ?? [] }, inputs:w.inputs ?? [], workflow:{ goal:w.workflow.goal, steps:w.workflow.steps }, commands:w.commands ?? {}, safety:{ allowedTools:w.safety?.allowedTools ?? [], never:w.safety?.never ?? [] }, outputs:{ successCriteria:w.outputs?.successCriteria ?? [] }, targets:{ claude:{enabled:w.targets?.claude?.enabled ?? true}, codex:{enabled:w.targets?.codex?.enabled ?? true}, agents:{enabled:w.targets?.agents?.enabled ?? true}, cursor:{enabled:w.targets?.cursor?.enabled ?? true}, copilot:{enabled:w.targets?.copilot?.enabled ?? true} } }; }
